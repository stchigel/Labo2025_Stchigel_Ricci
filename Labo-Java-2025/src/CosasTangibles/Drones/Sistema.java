/*package CosasTangibles.Drones;

import java.util.ArrayList;

public class Sistema {
    private ArrayList<Dron> drones;

    public Sistema() {
        this.drones = new ArrayList<>();
    }

    public Sistema(ArrayList<Dron> drones) {
        this.drones = drones;
    }

    public ArrayList<Dron> getDrones() {
        return drones;
    }

    public void setDrones(ArrayList<Dron> drones) {
        this.drones = drones;
    }

    public void addDron(Dron dron){
        drones.add(dron);
    }

    public void delDron(Dron dron){
        drones.remove(dron);
    }

    public Dron selDron(int i){
        return drones.get(i);
    }

    public void modDron(Dron dronOrig, Dron dronNew){
        for (int i = 0; i < drones.size(); i++) {
            if(drones.get(i).equals(dronOrig)){
                drones.set(i, dronNew);
            }
        }
    }
}*/
